apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: waf-data-pvc
  labels:
    app: waf-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nks-block-storage  # Naver Cloud NKS Block Storage